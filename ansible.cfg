[defaults]
# Where your list of servers lives
inventory = ansible/hosts.ini

# Very useful defaults for a home/lab setup
host_key_checking = False
# remote_user is not needed — your ~/.ssh/config already sets "User william" for all hosts
# timeout = 30
# forks = 10          # run up to 10 hosts in parallel (optional)
